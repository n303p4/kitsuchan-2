#!/usr/bin/env python3

from setuptools import setup

setup(name="owoe",
      version="0.0.1",
      description="A wrapper for weeb.sh",
      license="MIT",
      packages=["owoe"],
      zip_safe=False)
